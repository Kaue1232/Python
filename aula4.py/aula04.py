# Utilizando módulos. 
from math import sqrt #Vai utilizar apenas a funcionalidade sqrt (raiz quadrada)
número = int(input('Digite um número: '))
raiz = int(sqrt(número)) 

print('O valor da raiz quadrada de {} é {}'.format(número,raiz))

import random
num = random.randint(1,10)
print(num)

